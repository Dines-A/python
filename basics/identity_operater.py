#both are using same object
x=10
y=10
if x is y:
          print("both are using same object")
else:
          print("both are not using same objeect")

#both are not using same object    
x=10
y=20
if x is y:
          print("both are using same object")
else:
          print("both are not using same object")
#both are not using same object
x=10
y=20
if x is not  y:
          print("both are not using same object")
else:

          print("both are using same object")
#both are using same object

x=10
y=10
if x is not y:
          print("both are not using same object")
else:
          print("both are using same object")
